name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    # pinned to last known good SHA (main broke compat check after 2025-02-21)
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@9490391e1e4010c83291d437b7f9a71ace244581
